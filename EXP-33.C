#include <stdio.h>
#include <conio.h>

void main()
 {
  int i,j,temp,size,a[100];
  clrscr();
  printf("Enter the array size :\n");
  scanf("%d",&size);
  printf("Enter the array elements \n");
  for(i=0;i<size;i++)
   scanf("%d",&a[i]);
  for
  }